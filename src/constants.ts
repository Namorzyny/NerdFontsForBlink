import {resolve} from 'path';

export const DOWNLOAD_URL = 'https://www.nerdfonts.com/font-downloads';
export const REPO_PATH = `${resolve(__dirname)}/..`;
export const FONT_LIST = `${REPO_PATH}/fonts.json`;
